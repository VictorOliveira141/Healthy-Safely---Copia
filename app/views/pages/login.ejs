<html>
  <head>
    <title>Login</title>
    <link rel="stylesheet" href="../css/login.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link rel="icon" href="../imagem/Logo repaginado 2.png" />
  </head>

  <body>
    <%- include('../partials/header.ejs') %>
    <!---->
    <%- include('../partials/menu-inferior.ejs') %>

    <main class="main">
      <!-- Lado esquerdo -->
      <section class="lado-esquerdo">
        <h2>Sua saúde começa aqui!🍏</h2>
        <p>
          Cadastre-se e aproveite todos os benefícios que preparamos para você.
        </p>

        <ul class="beneficios">
          <li>✅ Compras mais rápidas</li>
          <li>✅ Descontos exclusivos</li>
          <li>✅ Histórico de pedidos</li>
          <li>✅ Promoções antecipadas</li>
        </ul>

        <!-- Slider automático -->
        <div class="slider">
          <div class="slide ativo">
            <blockquote>
              “Sempre encontro o que preciso e com ótimo atendimento!” — Maria
              S.
            </blockquote>
          </div>
          <div class="slide">
            <blockquote>
              “Preços justos e entrega rápida. Recomendo muito!” — João P.
            </blockquote>
          </div>
          <div class="slide">
            <blockquote>
              “Variedade incrível de produtos e promoções imperdíveis!” — Ana C.
            </blockquote>
          </div>
        </div>
      </section>

      <script src="../js/carrossel.js"></script>
      <!-- Fim lado esquerdo -->

      <section class="logo-mobile">
        <img src="../imagem/Group 13.png" alt="Logo" class="logo" />
      </section>

      <!-- Lado direito com formulário -->
      <section class="lado-direito">
        <article class="container">
          <form action="/login" method="POST" class="quadrado">
            <h1 class="novoUsuario">LOGIN</h1>
            <button type="submit" id="btnClique" class="google">
              <section>
                <img src="../imagem/google.png" class="googleimg" />
                <span>Continue com Google</span>
              </section>
            </button>

            <article class="formulario">
              <h2>Dados de acesso</h2>

              <article class="input-icon">
                <i class="bi bi-envelope icones-gerais"></i>
                <input
                  type="text"
                  id="usuarioDigitado"
                  name="usuarioDigitado"
                  placeholder="Digite seu email"
                  value="<%= valores?.usuarioDigitado || '' %>"
                  class="<%= erro ? 'erro' : '' %>"
                />
              </article>

              <article class="input-icon">
                <i class="bi bi-lock icones-gerais"></i>
                <input
                  type="password"
                  id="senhaDigitada"
                  name="senhaDigitada"
                  placeholder="Digite sua senha"
                  value="<%= valores?.senhaDigitada || '' %>"
                  class="<%= erro ? 'erro' : '' %>"
                />
                <i
                  class="bi bi-eye-slash-fill olhofechado"
                  onclick="mostrarSenha('senhaDigitada', this)"
                ></i>
              </article>

              <% if (erro) { %>
              <span class="msgErro"><%= erro %></span>
              <% } %>

              <h1 class="login">
                Ainda não possui uma conta?
                <a href="/cadastroCliente">Cadastre-se</a> já!
              </h1>

              <button type="submit" id="btnClique" class="juntar">
                Acessar
              </button>
            </article>
          </form>
        </article>
      </section>
    </main>

    <script src="../js/ver-senha.js"></script>

    <%- include('../partials/footer.ejs') %>
  </body>
</html>

<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sua Loja | Healthy Safely</title>
    <link rel="stylesheet" href="/css/adm/home2.css" />
    <link rel="icon" href="../imagem/Logo repaginado 2.png" />
  </head>

  <body class="admin-body">
    <%- include('../../partials/header.ejs') %>
    <!---->
    <%- include('../../partials/menu-inferior.ejs') %>

    <div class="admin-container">
      <!-- Sidebar -->
      <%- include('../../partials/adm/sidebar-loja.ejs') %>

      <!-- Main -->
      <main class="admin-main">
        <!-- Cabeçalho da loja -->
        <section class="loja-header">
          <img src="../imagem/atf.png" alt="Logo da Loja" class="logo-loja" />
          <div class="dados-loja">
            <h1><%= usuario.nomeFarmacia %></h1>
            <p>Sem descrição</p>
            <p class="localizacao">
              <i class="bi bi-geo-alt-fill"></i> Barueri - SP
            </p>
          </div>
          <article class="icones-editar">
            <button
              class="btn-editar-loja"
              onclick="window.location.href='/adm/paginaloja'"
            >
              <i class="bi bi-gear"></i>
            </button>
          </article>
        </section>

        <!-- Produtos da loja -->
        <section class="produtos-loja">
          <h2>Produtos Cadastrados</h2>

          <% if (produtos && produtos.length > 0) { %>
          <div class="lista-produtos">
            <% produtos.forEach(produto => { %>
            <article class="produto-card">
              <img src="<%= produto.imagem %>" alt="<%= produto.nome %>" />
              <h3><%= produto.nome %></h3>

              <div class="produto-info">
                <p><strong><%= produto.vendidos %></strong> vendidos</p>
                <p class="preco">
                  <span class="preco-antigo"><%= produto.precoAntigo %></span>
                  <span class="preco-novo"><%= produto.preco %></span>
                  <span class="desconto"><%= produto.desconto %></span>
                </p>
              </div>

              <button
                class="btn-editar"
                onclick="window.location.href='/adm/editarproduto'"
              >
                Editar <i class="bi bi-pencil"></i>
              </button>
            </article>

            <article class="produto-card">
              <img src="<%= produto.imagem %>" alt="<%= produto.nome %>" />
              <h3><%= produto.nome %></h3>

              <div class="produto-info">
                <p><strong><%= produto.vendidos %></strong> vendidos</p>
                <p class="preco">
                  <span class="preco-antigo"><%= produto.precoAntigo %></span>
                  <span class="preco-novo"><%= produto.preco %></span>
                  <span class="desconto"><%= produto.desconto %></span>
                </p>
              </div>

              <button
                class="btn-editar"
                onclick="window.location.href='/adm/editarproduto'"
              >
                Editar <i class="bi bi-pencil"></i>
              </button>
            </article>
            <% }) %>
          </div>
          <% } else { %>
          <div class="sem-produtos">
            <p>Você ainda não cadastrou nenhum produto 😔</p>
            <button
              class="btn-adicionar"
              onclick="window.location.href='/adm/adicionarproduto'"
            >
              Adicionar agora <i class="bi bi-plus-circle"></i>
            </button>
          </div>
          <% } %>
        </section>
      </main>
    </div>

    <%- include('../../partials/footer.ejs') %>
  </body>
</html>
